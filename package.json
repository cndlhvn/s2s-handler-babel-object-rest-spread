{
  "name": "s2s-handler-babel-object-rest-spread",
  "version": "0.0.1",
  "description": "s2s handler for babel syntax object rest spread",
  "main": "./lib/index.js",
  "scripts": {
    "build": "babel src --out-dir lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=6"
  },
  "license": "MIT",
  "dependencies": {
    "babel-core": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
